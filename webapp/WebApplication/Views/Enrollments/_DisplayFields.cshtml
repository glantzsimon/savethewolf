@using K9.Base.WebApplication.Helpers
@model Enrollment
@Html.BootstrapDisplayFor(model => model.StudentId)
@Html.BootstrapDisplayFor(model => model.CourseId)
@Html.BootstrapDisplayFor(model => model.Grade)