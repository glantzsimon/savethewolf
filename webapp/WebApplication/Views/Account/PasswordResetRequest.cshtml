@using K9.Base.WebApplication.Helpers
@model UserAccount.PasswordResetRequestModel
@{
    ViewBag.Title = Dictionary.PasswordResetTitle;
	Layout = "../Shared/_LayoutFormSmall.cshtml";
}

@using (Html.BeginForm())
{
	using (Html.BeginBootstrapForm())
	{
		@Html.BootstrapEditorFor(model => model.EmailAddress)
		@Html.BootstrapButton(Dictionary.ResetPassword)
	}
}