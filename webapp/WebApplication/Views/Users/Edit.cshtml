@using K9.Base.WebApplication.Helpers
@model User
@{
	Layout = "../Shared/_LayoutFormSmall.cshtml";
}
@using (Html.BeginForm())
{
	using (Html.BeginBootstrapForm())
	{
		@Html.HiddenFor(model => model.Id)
	    @Html.HiddenFor(model => model.IsDeleted)
		@Html.Partial("_EditFields")
		<hr/>
		@Html.BootstrapButton(Dictionary.Update)
		if (!WebSecurity.IsConfirmed(Model.Username))
		{
			@Html.BootstrapActionLinkButton(Dictionary.ActivateAccount, "ActivateUserAccount", "Account", new { userId = Model.Id })
		}
		@Html.BootstrapBackToListButton()
	}
}